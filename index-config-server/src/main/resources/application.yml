spring:
  application:
    name: index-config-server
  cloud:
    config:
      label: master
      server:
        git:
          uri: https://github.com/lds-java/springcloud_study
          searchPaths:
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/